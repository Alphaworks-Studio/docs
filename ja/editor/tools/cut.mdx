---
title: "メッシュカット"
description: "オプションのインターロックキー付きでモデルをピースに分割"
icon: "scissors"
---

メッシュカットは、平面に沿ってモデルをスライスし、別々のピースに分割します。プリンターに対して大きすぎるモデルや、セクションを個別に印刷して組み立てたい場合に不可欠です。

<Frame caption="カットツールでモデルをカットする">
  <video
    autoPlay
    loop
    muted
    playsInline
    controls
    poster="/images/editor/tools/cut-demo-poster.jpg"
    className="w-full rounded-2xl"
    src="/images/editor/tools/cut-demo.mp4"
  ></video>
</Frame>

## 使用するタイミング

- モデルが大きすぎて一度に印刷できない場合
- 個々のパーツを別々に塗装や仕上げしたい場合
- 小さなピースに分割してより高い解像度で印刷したい場合

## 設定

### 法線
**法線**プリセット（**+X**、**-X**、**+Y**、**-Y**、**+Z**、**-Z**）で平面の向きを設定するか、**X / Y / Z** 法線フィールドに正確な方向を入力します（値は自動的に正規化されます）。新しい平面は **+Y** 法線で始まります。

### 位置
**位置 X / Y / Z** フィールドで平面の位置を設定するか、**軸に沿った位置**スライダー（**0.00**〜**1.00**）をドラッグして、法線に沿ってモデル内で平面をスライドさせます。ビューポート内でギズモを使って平面を直接ドラッグすることもできます。ビューポートにプレビューが表示されます。

### 穴を閉じる
トグル（デフォルト：オン）。カットサーフェスを埋めて、各ピースをシールし印刷可能にします。

### 両側を保持
トグル（デフォルト：オン）。オンにすると、**両方**の半分が別々のピースとして保存されます。

## インターロックキー

**両側を保持**がオンの場合、印刷後にピースがスナップまたはフィットするインターロックキーを追加できます。

<Frame>
  <img src="/images/editor/tools/cut-keys.png" alt="4種類のインターロックキー：ピン、ダブテール、スナップ、パズル" />
</Frame>

### キータイプ

| タイプ | 説明 | 最適な用途 |
|------|-------------|----------|
| **ピン** | 円柱形のペグとホール | 汎用、最も印刷しやすい |
| **ダブテール** | 台形断面 | 強力、自動整列 |
| **スナップ** | マッシュルーム型のボールアンドソケット | クリックで固定、接着剤不要 |
| **パズル** | T字型のインターロック | 非常に強固、引き離しにくい |

### キー設定

- **キーを有効化** — カットサーフェスにキーを追加するトグル
- **キータイプ** — 上記の4タイプから選択
- **クリアランス** — キーとソケット間の隙間（0.05〜0.5 mm）。プリンターの公差に合わせて調整。
- **数** — 自動（自動配置）または固定数

## 2つのピース

キー付きのカットを実行すると、片側ごとに1つのシールされた印刷可能なピースが生成されます。それぞれに対応する位置合わせキーが付いており、組み立て時にパーツがきれいに合わさります。

<Columns cols={2}>
  <Frame caption="パーツ1 — 位置合わせキー付き">
    <img src="/images/editor/tools/cut-part-1.jpg" alt="1つ目のカットピース：カット面に位置合わせキーのペグが付いたベース" />
  </Frame>
  <Frame caption="パーツ2 — 対応するキー付き">
    <img src="/images/editor/tools/cut-part-2.jpg" alt="2つ目のカットピース：カット面に位置合わせキーが付いた上部セクション" />
  </Frame>
</Columns>

<Tip>
  ビューアの下にある **Cut Result** と **Cut Result Piece2** タブで結果を切り替えるか、**All** で両方を一緒に表示できます。
</Tip>

## 手順

<Steps>
  <Step title="カットツールを選択">
    ツールバーのインタラクティブツールセクションで**カット**をクリックします。
  </Step>
  <Step title="カットの向きを設定">
    法線プリセット（+X、-X、+Y、-Y、+Z、-Z）を選択するか、法線ベクトルを入力します。
  </Step>
  <Step title="カット平面を位置決め">
    軸に沿った位置スライダーまたはビューポート内の平面ギズモをドラッグ — プレビュー平面が表示されます。
  </Step>
  <Step title="両側を保持を有効化">
    両方の半分を保存したい場合はオンにします。
  </Step>
  <Step title="キーを追加（オプション）">
    キーを有効化し、組み立てに適したタイプを選択します。
  </Step>
  <Step title="カットを実行">
    **カットを実行**をクリックします。結果からピースをダウンロードします。
  </Step>
</Steps>

## ヒント

- どのタイプを使うか迷ったら**ピンキー**が最も安全な選択です
- FDMプリンターではキーのクリアランスを増やしてください（**0.2〜0.3 mm**）、レジンプリンターでは低いクリアランス（**0.05〜0.1 mm**）を使用してください
- ビューポートでカット位置を視覚的に確認してから実行してください
